# 트러블슈팅 정리 (2026-06-27)

프로젝트: Zero Check SBOM (SBOM/ZIP 업로드 → Syft/Grype 분석 → 취약점/정책 결과 제공)

---

## 1. Grype 취약점 DB 다운로드 시 디스크 용량 부족 (No space left on device)

### 문제 상황
백엔드(EC2, Spring Boot)에서 파일 분석 시 Grype가 취약점 DB를 받는 과정에서 `no space left on device` 오류로 분석이 계속 실패함. 백엔드 인스턴스의 루트 EBS 볼륨은 8GiB였음.

### 원인 분석
- Grype DB 자체가 얼마나 큰지 정확히 모른 채 "디스크가 부족하다"는 증상만 보고 있었음.
- Anchore 공식 블로그(grype-db schema v5→v6 변경 글)를 확인해 실제 요구 용량을 파악: v6 기준 압축 다운로드 65MB, 압축 해제 후 디스크 사용량 약 900MB(v5는 1.6GB).
- 실제 서버에서 다운로드를 받아보니 약 1.7GB까지 사용 — 블로그 기준치보다 컸음(DB가 그 사이 더 커졌거나 부가 오버헤드 존재).
- 루트 볼륨 8GiB 중 여유 공간이 Grype DB 하나만으로도 거의 소진되는 구조적 문제였음.

### 해결 과정
- 대안 비교: 루트 볼륨 자체를 늘리는 방법(비용 부담으로 보류) vs 막연한 정리로 공간 확보(불확실) vs 별도 소형 EBS 볼륨을 붙여서 Grype 캐시만 분리.
- 별도 2GiB gp3 EBS 볼륨(`vol-049c4c4278a201a70`)을 백엔드 인스턴스와 동일 가용 영역(ap-northeast-2c)에 생성 후 연결.
- Nitro 기반 인스턴스라 콘솔에서 지정한 디바이스명과 무관하게 OS에서는 NVMe 디바이스(`/dev/nvme1n1`)로 인식되는 점 확인.
- `ext4`로 포맷 → `/mnt/grype-cache`에 마운트 → `/etc/fstab`에 등록해 재부팅 후에도 유지.
- `~/.cache/grype`를 `/mnt/grype-cache`로 심볼릭 링크 연결 — Grype 설정(코드/옵션) 변경 없이 캐시 경로만 새 볼륨으로 우회시킴.

### 결과
- `grype db update` 정상 완료("Vulnerability database updated to latest version!").
- 새 볼륨에 1.7GB 사용, 루트 볼륨 여유 공간(929M)은 그대로 보존됨.
- 디스크 부족으로 인한 분석 실패(FAILED) 현상 해소.

---

## 2. 파일 분석/자동수정 ZIP 생성 시 임시 디렉토리 누수

### 문제 상황
1번 문제를 해결한 뒤, "분석 대상 파일이 계속 바뀌고 늘어날 텐데 디스크 문제가 다시 생기지 않을까?"라는 의문에서 출발해 코드를 점검함.

### 원인 분석
- `SbomFileService.java`에서 임시 디렉토리/파일을 생성하는 두 지점을 확인:
  - `analyzeFile()`의 zip 분석 분기: 업로드된 zip마다 `sbom-extract-*` 디렉토리(압축 해제용)와 `syft-sbom-*.json`(SBOM 생성 결과) 생성 — **모든 업로드 파일마다 실행되는 핵심 경로**.
  - 자동수정 ZIP 생성 메서드: `sbom-fix-source-*` 디렉토리 생성.
- 두 곳 모두 생성 후 삭제 로직이 전혀 없어 `/tmp`에 영구적으로 쌓이는 구조였음.
- 실제로 테스트 파일들로 인해 `sbom-extract-*` 디렉토리 8개가 `/tmp`에 잔존해 있는 것을 확인(현재는 작은 테스트 파일이라 용량은 작지만, 분석 건수가 늘수록 무한정 누적되는 구조).
- 참고: `/tmp/sbom-uploads/`에 원본 업로드 파일을 영구 보관하는 것은 파일 히스토리 기능을 위한 의도된 설계이며 이번 누수와는 무관, 손대지 않음.

### 해결 과정
- 지연 정리(크론 등)보다 즉시 정리(try/finally)가 적합하다고 판단 — 임시 디렉토리는 분석/압축이 끝나면 더 이상 쓸 일이 없는 순수 스크래치 공간이기 때문.
- 기존 의존성(commons-io 등) 추가 없이 `java.nio.file` 표준 API만으로 재귀 삭제 헬퍼(`deleteDirectoryQuietly`) 작성.
- `analyzeFile()`의 zip 분기와 자동수정 ZIP 생성 메서드 양쪽에 try/finally를 적용해, 정상/예외 상황 모두에서 임시 디렉토리·파일이 삭제되도록 수정.

### 결과
- 코드 패치 적용 후 백엔드 재빌드/재시작.
- 실제 사이트에서 파일 분석을 새로 돌린 뒤 `/tmp` 확인 — 기존 잔존 디렉토리 개수 그대로, 새로 생긴 디렉토리 없음(분석 완료 즉시 정리됨을 확인).
- 분석 건수가 늘어나도 `/tmp` 사용량이 무한정 누적되지 않도록 구조적으로 차단.
