# 트러블슈팅 정리 (2026-07-01)

프로젝트: Zero Check SBOM (SBOM/ZIP 업로드 → Syft/Grype 분석 → 취약점/정책 결과 제공)

---

## 1. GitHub Actions 헬스체크 타임아웃으로 배포 실패 후 롤백 반복

### 문제 상황

백엔드 CI/CD(GitHub Actions)에 배포 후 헬스체크 + 실패 시 롤백 로직을 추가했으나, 매번 헬스체크가 실패해 롤백이 반복 실행됨. 배포 자체(JAR 전송, systemd restart)는 성공하는데 헬스체크 단계에서만 계속 실패하는 상황.

### 원인 분석

- 기존 헬스체크는 12회 × 5초 = 최대 60초 대기 후 포트 응답이 없으면 실패 처리.
- `systemctl status safelink` 확인 결과 서비스는 `active (running)` 상태였지만 포트(18080)는 아직 열리지 않은 상태였음.
- t3.micro(1GB RAM) 환경에서 Spring Boot 애플리케이션이 JVM 기동 + Bean 초기화까지 완료하는 데 60초 이상 소요되는 경우가 발생함 — systemd가 프로세스 시작 즉시 `active`로 마킹하지만, 포트는 Spring Boot가 완전히 뜬 이후에 열리기 때문에 시간 차이가 존재.
- 추가로 기존 포트 체크 방식(`timeout 3 bash -c 'echo > /dev/tcp/localhost/18080'`)이 SSH 세션 내 서브셸에서 불안정하게 동작하는 경우가 있어 신뢰도가 낮았음.

### 해결 과정

- `/dev/tcp` 방식을 `curl`로 교체 — HTTP 응답 코드가 `000`(연결 실패)이 아니면 서버가 떴다고 판단, 4xx/5xx도 "서버가 살아있음"으로 간주해 헬스체크를 통과시킴.
- 헬스체크 시작 전 30초 초기 대기(sleep 30)를 추가해 Spring Boot 기동 시간을 확보.
- 이후 18회 × 5초 = 최대 90초 추가 대기 → 총 최대 2분 대기로 늘림.

```yaml
- name: 헬스체크
  run: |
    $SSH ubuntu@$BE_HOST "
      echo '⏳ 30초 대기 (Spring Boot 기동)...'
      sleep 30
      for i in \$(seq 1 18); do
        HTTP_CODE=\$(curl -s --max-time 5 -o /dev/null -w '%{http_code}' http://localhost:18080/ 2>/dev/null || echo '000')
        if systemctl is-active --quiet safelink && [ \"\$HTTP_CODE\" != '000' ]; then
          echo '✅ 헬스체크 통과 (HTTP '\$HTTP_CODE')'
          exit 0
        fi
        echo \"  \${i}/18 — HTTP \$HTTP_CODE, 5초 후 재시도...\"
        sleep 5
      done
      echo '❌ 헬스체크 실패 (총 2분 초과)'
      exit 1
    "
```

### 결과

- 헬스체크 42초 만에 통과, 전체 파이프라인 1분 28초 성공.
- 이후 배포 시 롤백 없이 정상 완료됨.

---

## 2. CI/CD 배포 후 백엔드 DB 연결 실패 (localhost:3306 Connection refused)

### 문제 상황

GitHub Actions CI/CD로 새 JAR을 배포한 뒤 `sudo journalctl -u safelink` 확인 시 아래 에러가 반복되며 서비스가 계속 죽고 재시작되는 현상 발생(restart counter: 57).

```
Caused by: java.sql.SQLNonTransientConnectionException:
Socket fail to connect to address=(host=127.0.0.1)(port=3306) Connection refused
```

### 원인 분석

- `application.properties`의 DB URL이 `jdbc:mariadb://127.0.0.1:3306/safelinkdb`로 하드코딩되어 있었음 — 로컬 개발 환경용 설정 그대로 커밋된 상태.
- 백엔드 EC2에는 로컬 MariaDB가 없고 AWS RDS에 연결해야 하는데, git에서 빌드된 JAR이 `localhost`로 연결을 시도하니 당연히 실패.
- `spring.datasource.username`, `spring.datasource.password`도 `${DB_USERNAME}`, `${DB_PASSWORD}` 환경변수로 참조하고 있었으나, systemd 서비스에 해당 환경변수가 설정되지 않아 null로 처리됨.
- 이전에는 수동으로 올린 JAR에 RDS 설정이 포함되어 있거나 셸 세션의 환경변수를 사용했기 때문에 동작했던 것으로 추정 — CI/CD 도입 후 이 설정이 사라진 것.

### 해결 과정

- 코드를 수정해 재배포하는 대신, systemd 서비스 파일에 환경변수를 주입하는 방식으로 해결 — 애플리케이션 재배포 없이 설정만 분리.
- Spring Boot는 `SPRING_DATASOURCE_URL` 환경변수를 자동으로 `spring.datasource.url` 프로퍼티에 매핑(relaxed binding)하므로, 코드 변경 없이 오버라이드 가능.

```ini
[Service]
Environment="DB_USERNAME=Ahnjungyeon"
Environment="DB_PASSWORD=roqudtls"
Environment="SPRING_DATASOURCE_URL=jdbc:mariadb://safe-sbom.c148s4gs2ngz.ap-northeast-2.rds.amazonaws.com:3306/safelinkdb?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true"
ExecStart=/usr/bin/java -jar /home/ubuntu/app.jar --server.port=18080
```

### 결과

- `sudo systemctl daemon-reload && sudo systemctl restart safelink` 후 서비스 정상 기동.
- RDS 연결 성공, 메모리 사용량 114MB → 231MB로 증가(DB 연결 풀 포함 정상 초기화 확인).
- 이후 재배포 시에도 systemd 환경변수는 유지되므로 설정이 날아가지 않음.

---

## 3. Google 로그인 404 → 500 (엔드포인트 미구현 + MEMB_EMAIL 누락)

### 문제 상황

`zerocheck-sbom.store`에서 Google 로그인 버튼 클릭 시 "구글 로그인 중 오류가 발생했습니다." 알림이 뜨며 로그인 실패. 브라우저 콘솔에서 아래 에러 확인:

```
Failed to load resource: 404 () — https://zerocheck-sbom.store/api/auth/google
```

이후 엔드포인트를 추가했으나 이번엔 500 에러로 바뀜.

### 원인 분석

**1단계 — 404 원인:**
- 프론트엔드는 Google Identity Services SDK로 ID 토큰을 받아 `POST /api/auth/google`로 전송하는 구조였음.
- nginx는 `/api/` → 백엔드(13.125.102.143:18080)로 프록시 설정이 되어 있었으나, 백엔드 `LoginController`에 `/api/auth/google` 엔드포인트 자체가 존재하지 않았음.
- CI/CD 도입 이전에 수동 배포했던 JAR에는 해당 엔드포인트가 있었으나, 코드가 git에 커밋되지 않은 상태였고 CI/CD는 git 기준으로 빌드하므로 누락된 것.
- `curl -s http://localhost:18080/v3/api-docs`로 실제 등록된 엔드포인트 확인 → `/api/auth/google` 없음 확인.

**2단계 — 500 원인:**
- `LoginController`에 `@PostMapping("/auth/google")` 추가, `MemberService`에 `googleLogin()` 메서드 구현 후 재배포.
- Google tokeninfo API(`https://oauth2.googleapis.com/tokeninfo?id_token={token}`)로 토큰 검증 후 이메일/이름 추출, 신규 회원이면 자동 가입하는 로직.
- 실제 Google 토큰으로 호출 시 500 발생 — `Member` 엔티티의 `MEMB_EMAIL` 컬럼이 `nullable = false`인데, 신규 회원 생성 시 `setMembEmail()`을 호출하지 않아 DB 제약 조건 위반으로 예외 발생.

### 해결 과정

- `MemberService.googleLogin()` 내 신규 회원 생성 람다에 `m.setMembEmail(email)` 한 줄 추가.
- Google 로그인 시 `membId`(로그인 ID)와 `membEmail` 모두 Google 이메일로 설정.

```java
Member member = memberRepository.findByMembId(email).orElseGet(() -> {
    Member m = new Member();
    m.setMembId(email);
    m.setMembNm(name);
    m.setMembEmail(email);  // 누락됐던 부분
    String salt = UUID.randomUUID().toString().substring(0, 8);
    m.setSalt(salt);
    m.setMembPwd(passwordEncoder.encode(UUID.randomUUID().toString() + salt));
    m.setLgnFa(0);
    m.setStatCd("1");
    return memberRepository.save(m);
});
```

### 결과

- Google 로그인 정상 작동 — 신규 회원 자동 가입 후 대시보드 진입 확인.
- 기존 회원(이메일로 이미 가입된 경우)도 중복 가입 없이 바로 로그인 처리됨.
